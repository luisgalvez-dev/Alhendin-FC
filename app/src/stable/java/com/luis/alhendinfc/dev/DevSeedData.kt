package com.luis.alhendinfc.dev

import android.content.Context

/** Variante stable: no hay datos de prueba. */
object DevSeedData {
    @Suppress("UNUSED_PARAMETER")
    suspend fun runIfNeeded(context: Context) = Unit
}
